pipeline {
    agent any

    stages {
        stage('Run Python Script') {
            steps {
                script {
                    echo "Running Python script..."
                    sh 'python -c "print(\'Hello, World!\')"'
                }
            }
        }
    }
}
